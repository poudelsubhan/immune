[project]
name = "immune"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "actian-vectorai-client>=1.0.2",
    "grpcio>=1.80",
    "numpy>=1.26",
    "pydantic>=2.10",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
